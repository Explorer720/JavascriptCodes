// js functions are the block of code that runs whenever called 

// eg 
// function starts with 'function' keword followed by functionName(parameters){}
function myFunction(x,y){
    console.log (x+y);
    console.log (x-y);
    console.log (x*y);
    console.log (x/y);

}

//whenever this function is called following given parameters , it executes

myFunction(10,5);
//
//

function sum(a,b){
    return a+b;
}

c = sum(25,75);
console.log(c);

//once the function is defined it can be used anywhere
